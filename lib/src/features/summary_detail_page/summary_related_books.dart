import 'package:flutter/cupertino.dart';

class SummaryRelatedBooks extends StatelessWidget{
  const SummaryRelatedBooks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Summary related books"),);
  }

}